<UserControl x:Class="QuizGameWPF.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizGameWPF"
             mc:Ignorable="d" 
             d:DesignHeight="640" 
             d:DesignWidth="360" 
             Background="LightBlue" 
             HorizontalAlignment="Center" 
             VerticalAlignment="Center">
    <Grid>
        <Border Margin="15,110,15,110" CornerRadius="10" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="90"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="70"/>
                </Grid.RowDefinitions>

                <!-- Heading row-->
                <Grid Grid.Row="0" Margin="250,15,15,15">
                    <Button Content="In Swedish" Margin="-20,0,0,-8">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Heading Welcome row-->
                <Grid Grid.Row="1">
                    <TextBox TextWrapping="Wrap" Text="Welcome to QUIZ game" 
                             FontWeight="Bold" 
                             FontSize ="25" 
                             VerticalAlignment="Center" 
                             HorizontalAlignment="Center" 
                             BorderThickness="0" />
                </Grid>
                <!-- Heading What to do row-->
                <Grid Grid.Row="2" Margin="15">
                    <TextBox TextWrapping="Wrap" 
                             Text="What do you want to do:" 
                             FontWeight="Bold" FontSize ="20" 
                             VerticalAlignment="Bottom" 
                             HorizontalAlignment="Center" BorderThickness="0" />
                </Grid>

                <!-- Play row -->
                <Grid Grid.Row="3" Margin="20,5,20,5">
                    <Button Content="Play" FontWeight="Bold" FontSize ="14" Click="Play_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Edit question row -->
                <Grid Grid.Row="4" Margin="20,5,20,5">
                    <Button Content="Edit existing questions" FontWeight="Bold" FontSize ="14" Click="Edit_Question_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Add new question row -->
                <Grid Grid.Row="5" Margin="20,5,20,5">
                    <Button Content="Add new question" FontWeight="Bold" FontSize ="14" Click="Add_Question_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</UserControl>
