<UserControl x:Class="QuizGameWPF.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizGameWPF"
             mc:Ignorable="d" 
             d:DesignHeight="640" 
             d:DesignWidth="360" 
             Background="LightBlue" 
             HorizontalAlignment="Center" 
             VerticalAlignment="Center">
    <Grid>
        <Border Margin="15,110,15,110" CornerRadius="10" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <!-- Heading Welcome row-->
                    <RowDefinition Height="140"/>
                    <!-- Heading What to do row-->
                    <RowDefinition Height="60"/>
                    <!-- Play row -->
                    <RowDefinition Height="50"/>
                    <!-- Edit quiz row -->
                    <RowDefinition Height="50"/>
                    <!-- Add new quiz row-->
                    <RowDefinition Height="50"/>
                    <!-- Empty row-->
                    <RowDefinition Height="70"/>
                </Grid.RowDefinitions>

                <!-- Heading Welcome row-->
                <Grid Grid.Row="0">
                    <TextBlock TextWrapping="Wrap" 
                               Margin="25,0,25,0"
                               x:Name="UserNameText"
                               FontWeight="Bold" 
                               FontSize ="25" 
                               TextAlignment="Center"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" 
                               />
                </Grid>
                <!-- Heading What to do row-->
                <Grid Grid.Row="1" Margin="15">
                    <TextBox TextWrapping="Wrap" 
                             Text="What do you want to do:" 
                             FontWeight="Bold" FontSize ="20" 
                             VerticalAlignment="Bottom" 
                             HorizontalAlignment="Center" BorderThickness="0" />
                </Grid>

                <!-- Play row -->
                <Grid Grid.Row="2" Margin="20,5,20,5">
                    <Button Content="Play a Quiz" FontWeight="Bold" FontSize ="14" Click="Play_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Edit quiz row -->
                <Grid Grid.Row="3" Margin="20,5,20,5">
                    <Button Content="Edit existing Quiz" FontWeight="Bold" FontSize ="14" Click="Edit_Question_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Add new quiz row -->
                <Grid Grid.Row="4" Margin="20,5,20,5">
                    <Button Content="Add new Quiz" FontWeight="Bold" FontSize ="14" Click="Add_Question_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
